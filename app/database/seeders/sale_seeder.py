# app/database/seeders/sale_seeder.py
import click

def seed_sale():
    """Placeholder for sales seeding. Currently does nothing as sales records are manually created.
    This function is currently not called by seed_all().
    """
    click.echo("Sales seeder: No automatic sales data to seed. Sales records are created manually via the admin interface.")